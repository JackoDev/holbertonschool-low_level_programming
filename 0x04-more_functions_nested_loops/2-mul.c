#include "holberton.h"

/**
 * mul - multiplies two integers.
 * @a: the int 1 to review
 * @b: the int 2 to review
 * Return: 1 if c is a digit
 */
int mul(int a, int b)
{
	int r = 0;

	r = a * b;
	return (r);
}
