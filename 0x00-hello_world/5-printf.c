#include<stdio.h>
/**
 * main - This function prints exactly a string with printf
 * Return: 0 so just print the text.
 */
int main(void)
{
	printf("%s\n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
