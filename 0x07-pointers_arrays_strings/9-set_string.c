#include "holberton.h"
#include <stddef.h>
#include <stdio.h>
/**
 * set_string - prints the chessboard.
 * @s: array to print
 * @to: the size of the diagonal
 * Return: its a void function
 */
void set_string(char **s, char *to)
{
	*s = to;
}
